{
  "name": "mkv-movie-library",
  "version": "1.0.0",
  "description": "Local webpage that scans your disks for .mkv movies and builds a poster library with IMDb details (via OMDb).",
  "type": "module",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "build": "node build.mjs"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@yao-pkg/pkg": "^6.21.0"
  }
}
